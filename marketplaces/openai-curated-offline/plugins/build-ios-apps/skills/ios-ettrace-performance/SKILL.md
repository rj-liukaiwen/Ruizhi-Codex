---
name: ios-ettrace-performance
description: Capture and interpret iOS Simulator ETTrace profiles. Use when profiling launch or runtime latency, comparing traces, or finding CPU-heavy stacks.
---

# Build iOS Apps: ios-ettrace-performance

Capture and interpret iOS Simulator ETTrace profiles. Use when profiling launch or runtime latency, comparing traces, or finding CPU-heavy stacks.

If this workflow requires an account-backed connector, use the installed app/connector for Build iOS Apps. If authorization is missing, ask the user to connect Build iOS Apps before taking account actions.
