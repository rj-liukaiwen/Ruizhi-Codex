---
name: airtable-filters
description: Use this skill when the user wants to find, filter, or narrow down Airtable records by field values, even when they don't explicitly say "filter."
---

# Airtable: airtable-filters

Use this skill when the user wants to find, filter, or narrow down Airtable records by field values, even when they don't explicitly say "filter."

If this workflow requires an account-backed connector, use the installed app/connector for Airtable. If authorization is missing, ask the user to connect Airtable before taking account actions.
