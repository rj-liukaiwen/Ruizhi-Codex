---
name: signing-entitlements
description: Inspect macOS signing, entitlements, and Gatekeeper issues. Use when diagnosing code signing, sandbox, hardened runtime, or trust failures.
---

# Build macOS Apps: signing-entitlements

Inspect macOS signing, entitlements, and Gatekeeper issues. Use when diagnosing code signing, sandbox, hardened runtime, or trust failures.

If this workflow requires an account-backed connector, use the installed app/connector for Build macOS Apps. If authorization is missing, ask the user to connect Build macOS Apps before taking account actions.
