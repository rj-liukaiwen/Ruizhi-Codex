---
name: react-three-fiber-game
description: Build React-hosted 3D browser games with React Three Fiber. Use when the user wants pmndrs-based scene composition, shared React state, and 3D HUD integration inside a React app.
---

# Game Studio: react-three-fiber-game

Build React-hosted 3D browser games with React Three Fiber. Use when the user wants pmndrs-based scene composition, shared React state, and 3D HUD integration inside a React app.

If this workflow requires an account-backed connector, use the installed app/connector for Game Studio. If authorization is missing, ask the user to connect Game Studio before taking account actions.
