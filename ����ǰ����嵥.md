# 锐捷发布前检查清单

日常开发只跑 `npm run sync:windows-test` 或 `npm run watch:windows-test`。只有确认发布新版本时，才按本清单做端到端验证。

## 当前补丁范围

- 固定 Windows 官方快照到 `vendor/codex-desktop/windows/current/app`，构建时通过 manifest 校验基线。
- Windows asar 使用 `overrides/windows-app/asar` 覆盖层，不直接修改官方快照。
- 应用名称、窗口标题、前端可见 Codex 文案改为锐捷，默认语言为 `zh-CN`。
- 主程序文件名保留 `Codex.exe`，快捷方式名称使用“锐捷Codex”。
- 使用 `RUIZHI_HOME` / `.ruizhi`，并兼容已有 `CODEX_HOME`。
- 启动时写入锐捷默认 `config.toml` 管理块：模型、锐擎 API provider、插件、Apps、Browser Use。
- APIKey 登录默认展示，隐藏 ChatGPT 登录入口，APIKey 输入框使用密码类型，并通过锐擎接口校验后写入认证状态。
- 模型列表替换为 `resources/ruizhi-model-catalog.json`，构建和测试同步时按当前 `resources/codex.exe --version` 自动写入匹配的 `client_version`。
- 内置本地 Responses bridge：GPT/Qwen 直通 UniAPI `/v1/responses`，Claude、GLM 5.1、Kimi K2.6、MiniMax M2.7、DeepSeek V4 通过 bridge 转换到 `/v1/chat/completions`。
- GPT 模型元数据必须标注 `input_modalities = ["text", "image"]`，避免上传/粘贴图片入口被前端禁用。
- 前端模型列表禁用官方 `available_models` 白名单过滤。
- 禁用官方 app sunset 强制更新拦截。
- 禁用官方更新入口，改用锐捷 Windows 更新通道。
- 设置页版本号位置显示当前版本、测试环境标记和更新进度；更新就绪时可点击安装。
- 系统托盘右键菜单和顶部菜单栏中文化。
- OpenAI 内置插件卡片、插件描述和 skill 触发描述中文化，保留 Browser、Chrome、LaTeX Tectonic 等产品名和关键术语。
- 内置 `imagegen` skill 覆盖和生图 helper，并为 helper 的固定生成子命令写入托管执行权限规则。
- 内置锐捷插件 marketplace，并为内置插件脚本写入托管执行权限规则。
- Windows 打包只输出 installer、带版本号 zip，以及 `dist/test-app/Codex.exe` 测试程序。
- Windows installer 清理旧 `锐捷.exe`，避免旧主程序残留。
- 可选重编 `resources/codex.exe` 时，优先使用 `.ruizhi` home、替换内置模型目录、替换内置 imagegen skill，并可禁用 OpenAI Responses WebSocket。

## 发布前端到端验证

- 启动 `dist/test-app/Codex.exe`，首次界面直接是中文。
- 前端关键品牌文案显示“锐捷”，不误伤模型名中的 Codex。
- 登录页默认进入 APIKey 表单，APIKey 校验不再出现 Authorization header 非法字符错误。
- 登录后模型列表显示锐捷模型目录，默认模型为 `gpt-5.5`，且模型缓存 `client_version` 与 `resources/codex.exe --version` 的整版本一致。
- 模型列表包含 `claude-opus-4-7`、`claude-sonnet-4-6`、`glm-5.1`、`kimi-k2.6`、`MiniMax/MiniMax-M2.7`、DeepSeek V4、Qwen3.6/Qwen3 Coder；不包含 `qwen3.5`、`glm-5`、`kimi-k2.5`、`MiniMax-M2.5`、DeepSeek v3。
- 选择 GPT 模型时可以上传或粘贴图片；选择文本模型时仍提示不支持图片。
- 选择一个 bridge 模型发送普通文本消息，能正常返回，不出现 `/v1/responses` 不兼容错误。
- 设置页右侧版本号显示正常，点击版本号/更新状态不会跳到 APIKey 页面。
- 测试版显示测试环境版本标记，正式 installer 不显示测试环境标记。
- 系统托盘右键菜单和顶部菜单栏为中文。
- OpenAI 内置插件描述显示为中文，产品名和关键术语未被强行翻译。
- 官方 app sunset 页面不会出现。
- 插件、Apps、Browser Use 在 APIKey 模式下可用。
- imagegen skill 能调用内置 helper，且不再弹执行权限授权。
- 内置锐捷插件脚本执行时不再弹执行权限授权。
- installer 安装后快捷方式名称为“锐捷”，运行进程主程序名为 `Codex.exe`。
- `dist/installer` 只包含发布需要的 installer、带版本号 zip 和更新清单，不生成多余 `锐捷Codex-Setup` 副本。
